# launch config — calc(server)と asker(client)をまとめて起動する。
#
#   reiny ping-pong-service.toml --bin-dir target/debug
#
[grain]
calc = { bin = "calc", on_exit = "respawn" }
asker = { bin = "asker", depends_on = ["calc"] }
