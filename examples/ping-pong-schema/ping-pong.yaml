# launch config — ping/pong をまとめて起動する(../ping-pong-workspace と同じ)。
# 共有スキーマクレートはライブラリなので launch には現れない(ビルド時に共有されるだけ)。
#
#   reiny ping-pong.toml
#
[launch]
pong = { bin = "pong", on_exit = "respawn" }
ping = { bin = "ping", depends_on = ["pong"] }
