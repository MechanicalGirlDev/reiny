syntax = "proto3";
package hos;
import "types/geometry.proto";

// 単一フレームの相対変換（ROS tf2 相当）。
// 今後 TransformStamped / TransformTree 等へ汎用拡張する基盤型。
message Transform {
  string frame_id = 1;
  string child_frame_id = 2;
  Pose transform = 3;
}
